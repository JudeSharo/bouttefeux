{% extends 'base.html.twig' %}

{% block title %}Edit de {{ app.user.getPseudo}} {% endblock %}

{% block body %}
    <div class="belowNavbar">
        <h1>Edit Event</h1>

        {{ include('profil/_form.html.twig', {'button_label': 'Update'}) }}

        <a href="{{ path('index') }}">back to list</a>

        {{ include('profil/_delete_form.html.twig') }}
    </div>

{% endblock %}